# Backend URL — points to FastAPI service. In docker-compose this becomes
# http://api:8000 (container DNS). For local-only dev outside docker use
# http://localhost:8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
