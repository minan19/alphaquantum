import type { ReactNode } from "react";

export const metadata = { title: "UI Gallery — M1 token-bağlı bileşenler" };

export default function GalleryLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
